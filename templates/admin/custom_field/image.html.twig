{# @var ea \EasyCorp\Bundle\EasyAdminBundle\Context\AdminContext #}
{# @var field \EasyCorp\Bundle\EasyAdminBundle\Dto\FieldDto #}
{# @var entity \EasyCorp\Bundle\EasyAdminBundle\Dto\EntityDto #}
{% set images = field.formattedValue %}
{% if images is not iterable %}
    {% set images = [images] %}
{% endif %}

{# correction bug click zoom image #}
{% for image in images %}
    <img src="{{ asset(image) }}" class="img-fluid" style="box-shadow: 0 0 0 2px white,0 0 4px 1px #878f97;margin-bottom: 2px;margin-top: 2px;max-height: 100px;max-width: 150px;">
{% endfor %}
{# ancien template : #}
{# {% set html_id = 'ea-lightbox-' ~ field.uniqueId ~ '-' ~ loop.index %}
<a href="#" class="ea-lightbox-thumbnail" data-featherlight="#{{ html_id }}" data-featherlight-close-on-click="anywhere">
    <img src="{{ asset(image) }}" class="img-fluid">
</a>

<div id="{{ html_id }}" class="ea-lightbox">
    <img src="{{ asset(image) }}">
</div> #}